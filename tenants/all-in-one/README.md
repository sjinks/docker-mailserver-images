# All Services On One Host

This sample is for a small new deployment where one server owns every service.
It is a placement example only. Add complete tenant configuration and fresh
secret files before running the rendered Compose bundle.