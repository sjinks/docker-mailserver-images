# One Service Per Host

This sample assigns every service to its own server. Every service endpoint must
be configured through private DNS and firewall rules. Add host-specific service
configuration and fresh secret files before deployment.